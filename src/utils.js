const MAX_ROOMS = 5;
const MAX_PERSONS = 5;
const MIN_ADULTS = 1;
const MIN_CHILDREN = 0;

// let ad = {
//   rooms : 0,
//   adults: 0,
//   children: 0,
// }

const getState = (newState) => {
  const { rooms, adults, children } = newState;
  // if ()
};
